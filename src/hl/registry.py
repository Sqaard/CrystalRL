"""HL v2 DynamicRegistry -- superseded stub.

This file was a truncated draft; the real self-expanding, class-typed knob
registry is src/hl/registry_v2.py (import that instead). Kept as a stub so the
package compiles.
"""
